<?xml version="1.0" encoding="UTF-8" ?>
<section xml:id="sec-rate-change-tangent-curves" class="notes">
  <title>1.6 Rate of Change and Tangent Curves</title>
  <p>
    The rate of change of a function at a point is the slope of the tangent line to the graph of the function at that point. It can be calculated using limits.
  </p>
  
  <p>
  If I drive 200 miles in 4 hours, my average velocity is <m>50</m> miles per hour. 
  Let <m>s(t)</m> represent the reading on my odometer at time <m>t</m>. 
  Suppose I want to find my average velocity during some time interval that begins when <m>t=a</m> and ends <m>h</m> hours later. 
  Velocity can be calculated as follows:
  <me>v_{\text{ave}} = \frac{\text{change in distance}}{\text{change in time}} = \frac{s(a+h)-s(a)}{h}</me>
  The subscript <q>ave</q> tells us this is an average velocity. 
  My velocity does not have to be constant during this time (Even cruise control is not perfect).
  </p>
  <p>
  Our common notion of velocity involves a change in distance and a change in time. 
  Yet, a speedometer tells us the velocity of a car at any instant. 
  We can do this mathematically using the idea of a limit.
</p>

<exercise>
  <statement>
    <p>
      Suppose <m>s(t) = t^2</m>. Complete the following table, letting <m>a=3</m>.
    </p>
    <table xml:id="table-velocity-limit">
      <title>Calculating Average Velocity as <m>h \to 0</m></title>
      <tabular bottom="minor" top="minor" left="minor" right="minor">
        <row header="yes">
          <cell><m>h</m></cell>
          <cell><m>s(3+h)</m></cell>
          <cell><m>s(3)</m></cell>
          <cell><m>s(3+h)-s(3)</m></cell>
          <cell><m>\frac{s(3+h)-s(3)}{h}</m></cell>
        </row>
        <row>
          <cell>1</cell>
          <cell>16</cell>
          <cell>9</cell>
          <cell>7</cell>
          <cell>7</cell>
        </row>
        <row>
          <cell>0.1</cell>
          <cell>9.61</cell>
          <cell>9</cell>
          <cell>0.61</cell>
          <cell>6.1</cell>
        </row>
        <row>
          <cell>0.01</cell>
          <cell>9.0601</cell>
          <cell>9</cell>
          <cell>0.0601</cell>
          <cell>6.01</cell>
        </row>
        <row>
          <cell>0.001</cell>
          <cell>9.006001</cell>
          <cell>9</cell>
          <cell>0.006001</cell>
          <cell>6.001</cell>
        </row>
      </tabular>
    </table>
    <p>
      Each of the values in the last column represents an average velocity. What seems to be happening to these values as <m>h</m> gets smaller?
    </p>
    <p>
      So when <m>a=3</m>, <m>v_{\text{ins}} = </m> <fillin characters="5" />.
    </p>
  </statement>
  <solution>
    <p>
      As <m>h</m> approaches <m>0</m>, the values in the last column seem to be approaching <m>6</m>. Therefore, when <m>a=3</m>, the instantaneous velocity is <m>v_{\text{ins}} = 6</m>.
    </p>
  </solution>
</exercise>

<assemblage xml:id="asm-average-rate-of-change">
  <title>Average Rate of Change</title>
    <p>
      The <term>average rate of change of <m>y</m> with respect to <m>x</m></term>, as <m>x</m> changes from <m>x_1</m> to <m>x_2</m>, is the ratio of the change in output to the change in input:
      <me>
        \frac{y_2-y_1}{x_2-x_1} = \frac{f(x_2)-f(x_1)}{x_2-x_1}, \quad \text{where } x_2 \neq x_1
      </me>
    </p>
</assemblage>

<p>
  The slope of the line from <m>P(x_1,y_1)</m> to <m>Q(x_2,y_2)</m> is <m>\displaystyle \frac{f(x_2)-f(x_1)}{x_2-x_1}</m> 
  and the line <m>\overleftrightarrow{PQ}</m> is called a <term>secant line</term>.
</p>

<definition xml:id="def-tangent-line">
  <title>Tangent Line</title>
  <statement>
    <p>
      The <term>Tangent line</term> to the curve <m>y=f(x)</m> at the point <m>P(a,f(a))</m> is the line through <m>P</m> with slope
      <me>
        m = \lim_{x \to a} \frac{f(x)-f(a)}{x-a}
      </me>
      provided that this limit exists.
    </p>
  </statement>
</definition>
<p>
  We sometimes refer to the slope of the tangent line to the curve at a point as the <term>slope of the curve</term> at the point.
</p>

<example xml:id="ex-tangent-parabola">
  <statement>
    <p>
      Find the equation of the tangent line to the parabola <m>y=x^2</m> at the point <m>P(-2,4)</m>.
    </p>
    <figure xml:id="fig-parabola-tangent">
  <caption>The parabola <m>y=x^2</m> and its tangent line at <m>P(-2,4)</m></caption>
  <image xml:id="img-parabola-tangent" width="60%">
    <shortdescription>A parabola opening upward with a tangent line touching it at x equals negative 2.</shortdescription>
    <latex-image>
      \begin{tikzpicture}[scale=0.8]
        % Grid and Axes
        \draw[step=1, gray!20, thin] (-5,-1) grid (2,6);
        \draw[-stealth] (-5.2,0) -- (2.2,0) node[right] {\(x\)};
        \draw[-stealth] (0,-1.2) -- (0,6.2) node[above] {\(y\)};

        % Axis Labels
        \foreach \x in {-4,-2,2} \draw (\x,0.1) -- (\x,-0.1) node[below] {\small \(\x\)};
        \foreach \y in {2,4,6} \draw (0.1,\y) -- (-0.1,\y) node[left] {\small \(\y\)};

        % The Parabola y = x^2
        \draw[very thick, blue, domain=-2.4:2.4, smooth] plot (\x, {\x*\x});
        \node[blue, right] at (1.5, 2.25) {\(y=x^2\)};

        % The Tangent Line y = -4x - 4
        % When x = -2.5, y = 6. When x = -1, y = 0.
        \draw[thick, red, domain=-2.5:-0.5] plot (\x, {-4*\x - 4});
        \node[red, left] at (-2.5, 6) {\(y=-4x-4\)};

        % Point of Tangency
        \fill[black] (-2,4) circle (2.5pt) node[above left] {\(P(-2,4)\)};
      \end{tikzpicture}
    </latex-image>
  </image>
</figure>

  </statement>
  <solution>
  <title>Method using <m>x \to a</m></title>
  <p>
    We can also find the slope <m>m</m> at <m>a = -2</m> using the alternative limit definition:
    <md>
      <mrow>m \amp = \lim_{x \to a} \frac{f(x) - f(a)}{x - a}</mrow>
      <mrow>\amp = \lim_{x \to -2} \frac{x^2 - (-2)^2}{x - (-2)}</mrow>
      <mrow>\amp = \lim_{x \to -2} \frac{x^2 - 4}{x + 2}</mrow>
    </md>
  </p>
  <p>
    Factoring the numerator as a difference of squares:
    <md>
      <mrow>\amp = \lim_{x \to -2} \frac{(x + 2)(x - 2)}{x + 2}</mrow>
      <mrow>\amp = \lim_{x \to -2} (x - 2)</mrow>
      <mrow>\amp = -2 - 2 = -4</mrow>
    </md>
  </p>
  <p>
    This gives the same slope, <m>m = -4</m>, which we then use in the point-slope formula to find <m>y = -4x - 4</m>.
  </p>
</solution>

</example>

<p>
  The average rate of change is called a <term>difference quotient</term>, given by:
  <me>
    \frac{y_2-y_1}{x_2-x_1} = \frac{f(x_2)-f(x_1)}{x_2-x_1} = \frac{f(x+h)-f(x)}{(x+h)-x} = \frac{f(x+h)-f(x)}{h}
  </me>
</p>
<p>
  We now use a different notation for average rates of change by eliminating the subscripts. 
  Let <m>x</m> be <m>x_1</m> and <m>x+h</m> be <m>x_2</m>. 
  To get from <m>x_1</m> to <m>x_2</m>, we calculate the change in input as 
  <m>x_2 - x_1 = (x+h) - x = h</m>. 
</p>

<definition xml:id="def-difference-quotient-tangent">
  <title>Difference Quotient and Tangent Slope</title>
  <statement>
    <p>
      The average rate of change of <m>f</m> with respect to <m>x</m> is also called the <term>difference quotient</term>. It is given by:
      <me>
        \frac{f(x+h)-f(x)}{h}, \quad \text{where } h \neq 0
      </me>
    </p>
    <p>
      The difference quotient is equal to the slope of the line from a point <m>P(x,f(x))</m> to a point <m>Q(x+h,f(x+h))</m>. The slope of the tangent line at <m>x=a</m> is then:
      <me>
        m = \lim_{h \to 0} \frac{f(a+h)-f(a)}{h}
      </me>
    </p>
  </statement>
</definition>

  <example xml:id="ex-rate-change">
    <title>Rate of Change of a Quadratic Function</title>
    <p>
      Consider the function <me>f(x) = x^2</me>. 
      We want to find the rate of change at the point <me>x=-2</me>.
    </p>
    <p>
      Using the definition, we have:
    </p>
    <p>
      <me>\lim_{h \to 0} \frac{(-2+h)^2 - (-2)^2}{h} = \lim_{h \to 0} \frac{4 - 4h + h^2 - 4}{h} = \lim_{h \to 0} \frac{-4h + h^2}{h} = \lim_{h \to 0} (-4 + h) = -4</me>
    </p>
    <p>
      Therefore, the rate of change of the function at <me>x=-2</me> is -4.
    </p>
  </example>
  <example xml:id="ex-slope-reciprocal-squared">
  <statement>
    <p>
      Find the slope of the curve <m>y = 1/x^2</m> at <m>x = 1/2</m>.
    </p>
  </statement>
  <solution>
    <p>
      First, we find the <m>y</m>-coordinate of the point:
      <me>y = f(1/2) = \frac{1}{(1/2)^2} = 4</me>
    </p>
    <p>
      Then, we find a formula for the <m>y</m>-coordinate at a point near <m>x = 1/2</m>. By squaring the binomial and simplifying:
      <me>f(1/2+h) = \frac{1}{(1/2+h)^2} = \frac{1}{1/4 + h + h^2} = \frac{4}{1 + 4h + 4h^2}</me>
    </p>
    <p>
      Now, we evaluate the limit of the difference quotient:
      <md>
        <mrow>f'(1/2) \amp = \lim_{h\to 0} \frac{\frac{4}{1 + 4h + 4h^2} - 4}{h}</mrow>
        <mrow>\amp = \lim_{h\to 0} \frac{\frac{4}{1 + 4h + 4h^2} - \frac{4(1 + 4h + 4h^2)}{1 + 4h + 4h^2}}{h}</mrow>
        <mrow>\amp = \lim_{h\to 0} \frac{\frac{4 - 4 - 16h - 16h^2}{1 + 4h + 4h^2}}{h}</mrow>
        <mrow>\amp = \lim_{h\to 0} \frac{-16h - 16h^2}{h(1 + 4h + 4h^2)}</mrow>
        <mrow>\amp = \lim_{h\to 0} \frac{-16 - 16h}{1 + 4h + 4h^2}</mrow>
        <mrow>\amp = \frac{-16 - 0}{1 + 0 + 0} = -16</mrow>
      </md>
    </p>
    <p>
      Our calculation shows that the slope of the line tangent to <m>y = 1/x^2</m> at <m>x = 1/2</m> is <m>-16</m>.
    </p>
  </solution>
</example>
<example xml:id="ex-simplified-difference-quotient">
  <statement>
    <p>
      For <m>f(x)=x^2</m>, find a <term>simplified</term> form of the <term>difference quotient</term>.
    </p>
  </statement>
  <solution>
    <p>
      Recall that the difference quotient is given by <m>\displaystyle \frac{f(x+h)-f(x)}{h}</m>. Substituting <m>f(x)=x^2</m>, we have:
      <md>
        <mrow>\frac{f(x+h)-f(x)}{h} \amp = \frac{(x+h)^2 - x^2}{h}</mrow>
        <mrow>\amp = \frac{(x^2 + 2xh + h^2) - x^2}{h}</mrow>
        <mrow>\amp = \frac{2xh + h^2}{h}</mrow>
        <mrow>\amp = \frac{h(2x + h)}{h}</mrow>
        <mrow>\amp = 2x + h, \quad \text{for } h \neq 0</mrow>
      </md>
    </p>
  </solution>
</example>
<p>
  By taking the limit of the result from <xref ref="ex-simplified-difference-quotient" /> as <m>h \to 0</m>, we find the derivative (the instantaneous rate of change) of the function.
</p>

<assemblage xml:id="asm-derivative-x-squared">
  <title>The Derivative of <m>x^2</m></title>
    <p>
      The derivative of <m>f(x) = x^2</m>, denoted as <m>f'(x)</m>, is calculated as:
      <md>
        <mrow>f'(x) \amp = \lim_{h \to 0} \frac{f(x+h) - f(x)}{h}</mrow>
        <mrow>\amp = \lim_{h \to 0} (2x + h)</mrow>
        <mrow>\amp = 2x</mrow>
      </md>
    </p>
</assemblage>

<p>
  This result, <m>f'(x) = 2x</m>, provides a formula to find the slope of the tangent line to the parabola at <em>any</em> point <m>x</m>.
</p>
<example xml:id="ex-diff-quotient-cubic">
  <statement>
    <p>
      For <m>f(x)=x^3</m>, find a simplified form of the difference quotient.
    </p>
  </statement>
  <solution>
    <p>
      We substitute <m>f(x)=x^3</m> into the difference quotient formula:
      <md>
        <mrow>\frac{f(x+h)-f(x)}{h} \amp = \frac{(x+h)^3 - x^3}{h}</mrow>
        <mrow>\amp = \frac{(x^3 + 3x^2h + 3xh^2 + h^3) - x^3}{h}</mrow>
        <mrow>\amp = \frac{3x^2h + 3xh^2 + h^3}{h}</mrow>
        <mrow>\amp = \frac{h(3x^2 + 3xh + h^2)}{h}</mrow>
        <mrow>\amp = 3x^2 + 3xh + h^2, \quad \text{for } h \neq 0</mrow>
      </md>
    </p>
  </solution>
</example>
<p>
  By taking the limit of the result from <xref ref="ex-diff-quotient-cubic" /> as <m>h \to 0</m>, we find the derivative (the instantaneous rate of change) of the function.
</p>

<p>
  This result, <m>f'(x) = \lim_{h \to 0} (3x^2 + 3xh + h^2)=3x^2</m>, 
  provides a formula to find the slope of the tangent line to the cubic curve at <em>any</em> point <m>x</m>.
</p>
<exercise xml:id="ex-diff-quotient-reciprocal">
  <task>
    <statement>
      <p>
        For <m>f(x)=3/x</m>, find a simplified form of the difference quotient.
      </p>
    </statement>
    <solution>
      <p>
        We substitute <m>f(x) = \frac{3}{x}</m> into the difference quotient formula:
        <md>
          <mrow>\frac{f(x+h)-f(x)}{h} \amp = \frac{\frac{3}{x+h} - \frac{3}{x}}{h}</mrow>
          <mrow>\amp = \frac{1}{h} \left( \frac{3x - 3(x+h)}{x(x+h)} \right)</mrow>
          <mrow>\amp = \frac{1}{h} \left( \frac{3x - 3x - 3h}{x(x+h)} \right)</mrow>
          <mrow>\amp = \frac{-3h}{h \cdot x(x+h)}</mrow>
          <mrow>\amp = \frac{-3}{x(x+h)}, \quad \text{for } h \neq 0</mrow>
        </md>
      </p>
    </solution>
  </task>

  <task>
    <statement>
      <p>
        Find the equation of the tangent line to the hyperbola <m>y=3/x</m> at the point <m>P(-1,-3)</m>.
      </p>
    </statement>
    <solution>
        <figure xml:id="fig-hyperbola-tangent">
  <caption>The hyperbola <m>y=3/x</m> and its tangent line at <m>P(-1,-3)</m></caption>
  <image xml:id="img-hyperbola-tangent" width="60%">
    <shortdescription>A hyperbola in the first and third quadrants with a tangent line at negative 1, negative 3.</shortdescription>
    <latex-image>
      \begin{tikzpicture}[scale=0.8]
        % Grid and Axes
        \draw[step=1, gray!20, thin] (-6,-6) grid (2,2);
        \draw[-stealth] (-6.2,0) -- (2.2,0) node[right] {\(x\)};
        \draw[-stealth] (0,-6.2) -- (0,2.2) node[above] {\(y\)};

        % Axis Labels
        \foreach \x in {-6,-4,-2,2} \draw (\x,0.1) -- (\x,-0.1) node[below] {\small \(\x\)};
        \foreach \y in {-6,-4,-2,2} \draw (0.1,\y) -- (-0.1,\y) node[left] {\small \(\y\)};

        % Hyperbola y = 3/x (Third Quadrant)
        \draw[very thick, blue, domain=-6:-0.5, smooth] plot (\x, {3/\x});
        \node[blue, left] at (-4, -0.75) {\(y=3/x\)};

        % Tangent Line y = -3x - 6
        % When x = -2, y = 0. When x = 0, y = -6.
        \draw[thick, red, domain=-2.2:-0.2] plot (\x, {-3*\x - 6});
        \node[red, right] at (-0.5, -4.5) {\(y=-3x-6\)};

        % Point of Tangency
        \fill[black] (-1,-3) circle (2.5pt) node[below left] {\(P(-1,-3)\)};
      \end{tikzpicture}
    </latex-image>
  </image>
</figure>

      <p>
        First, we find the slope <m>m</m> at <m>x = -1</m> by taking the limit of the difference quotient from the previous part:
        <md>
          <mrow>m \amp = \lim_{h \to 0} \frac{-3}{x(x+h)} \bigg|_{x=-1}</mrow>
          <mrow>\amp = \lim_{h \to 0} \frac{-3}{-1(-1+h)}</mrow>
          <mrow>\amp = \frac{-3}{(-1)(-1)} = -3</mrow>
        </md>
      </p>
      <p>
        Now, using the point-slope form with <m>P(-1, -3)</m> and <m>m = -3</m>:
        <md>
          <mrow>y - (-3) \amp = -3(x - (-1))</mrow>
          <mrow>y + 3 \amp = -3x - 3</mrow>
          <mrow>y \amp = -3x - 6</mrow>
        </md>
      </p>
      <p>
        The equation of the tangent line is <m>y = -3x - 6</m>.
      </p>
    </solution>
  </task>
</exercise>





</section>  
